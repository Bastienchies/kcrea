<!DOCTYPE html>
<html>
<body>
{% include 'common/navLeft.html.twig' %}
{% include 'common/navTop.html.twig' %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<link href="{{ asset('css/api_mediaStyle.css') }}" rel="stylesheet" />

<div style="width: 100%; margin-left: 50%; margin-top: 5%">

{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}

</div>
<div class="br-mainpanel">
    <table>
        {% if research is defined %}
        {% for film in research %}
        <tr>

            {{ film.getPoster() }}
            <center><h1>{{ film.getTitre() }}</h1></center>
            <center><h2>{{ film.getYear() }}</h2></center>
            <br>
        </tr>
        {% endfor %}
        {% endif %}
    </table>
</div>


<script src="{{ asset('lib/jquery/jquery.js') }}"></script>
</body>
{% include 'common/footer.html.twig' %}
</html>
